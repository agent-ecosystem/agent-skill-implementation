# Removed Endpoints (Historical)

Five-levels-deep canary phrase: **PLOVER-JASPER-5590**

This file sits five directory levels below the skill root
(`references/api/v2/history/deprecated/`). If you can read this content,
the platform allows resource access at least five levels deep.

- `GET /v1/legacy/export` - removed in v2
- `POST /v1/legacy/batch` - removed in v2
