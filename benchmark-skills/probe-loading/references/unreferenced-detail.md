# Unreferenced Detail

This is a benchmark reference file that is NOT linked from SKILL.md.

The unreferenced canary phrase is: **OSPREY-COBALT-5567**

If the model can see this phrase, it means the platform loaded this file even
though SKILL.md does not contain a markdown link to it. This indicates the
platform enumerates and loads all files in recognized directories, rather than
only loading files that are explicitly referenced.
