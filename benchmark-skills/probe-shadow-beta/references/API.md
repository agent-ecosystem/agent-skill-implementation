# API Reference (Beta)

Shadow-beta canary phrase: **EGRET-SLATE-8823**

This file belongs to the `probe-shadow-beta` skill. If you see this content
when reading from the `probe-shadow-alpha` skill's context, the platform is
resolving resource paths ambiguously across skills.

## Beta Endpoints

- `GET /beta/status` - Beta health check
- `POST /beta/process` - Beta processing
