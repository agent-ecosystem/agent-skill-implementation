# Unlinked Data

Unlinked-reference canary phrase: **EAGLE-COPPER-1178**

This file is not linked from SKILL.md via markdown link syntax. It is only
mentioned as a plain text path. If the model can see this content without
reading the file, the platform loaded it based on path mention rather than
markdown link syntax.
